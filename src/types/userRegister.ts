export type userRegister = {
  email: string,
  password: string,
  repeatPassword: string,
  firstName: string,
  lastName: string,
};

export type userLogin = {
  email: string,
  password: string,
};